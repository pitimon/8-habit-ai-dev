# Self-Check: 8-Habit Cross-Verification on This Plugin

**Version**: 2.21.33 | **Date**: 2026-06-20 | **Previous**: 2.21.32 (Evenhandedness/steelman + behavioral-spec craft + staleness honesty — #325)

Running our own 17-question checklist against the plugin itself. H8 Modeling: "Follow the process always, no shortcuts when unwatched."

## Private Victory (Self-Management)

| #   | Habit            | Dimension   | Question                                    | Result | Evidence                                                                                                                                                            |
| --- | ---------------- | ----------- | ------------------------------------------- | ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | H1: Be Proactive | Body+Spirit | Checked what else this change affects?      | Pass   | Explore agent scanned all 17 skills, 8 habits before any edits. Gap analysis found 3 convention violations, version drift, missing CI                               |
| 2   | H1: Be Proactive | Body        | Considered edge cases?                      | Pass   | validate-structure.sh checks 5 edge cases: missing frontmatter, name mismatch, missing sections, version drift, file size. Previously: Fail (no validation existed) |
| 3   | H1: Be Proactive | Body        | Documentation updated as part of change?    | Pass   | CLAUDE.md, CONTRIBUTING.md, README.md, SELF-CHECK.md all updated alongside code changes                                                                             |
| 4   | H2: End in Mind  | Mind        | 3-5 concrete success criteria?              | Pass   | Every issue has Acceptance Criteria with 3-5 checkboxes. Verification section in plan has 6 specific criteria                                                       |
| 5   | H2: End in Mind  | Mind        | Test plan with verification steps?          | Pass   | `bash tests/validate-structure.sh` as concrete CI verification. Previously: Fail (no tests existed at all)                                                          |
| 6   | H2: End in Mind  | Mind        | Commit messages explain WHY?                | Pass   | Commits reference issue numbers, research basis, and dimension targets                                                                                              |
| 7   | H3: First Things | Mind        | Most important thing, not most interesting? | Pass   | Body (CI) was highest-impact gap, done first. NOT adding markdownlint (interesting but would add npm dependency)                                                    |
| 8   | H3: First Things | Heart       | Resisted scope creep?                       | Pass   | 6 issues for gap fixes, "NOT in scope" section explicitly excludes 4 items                                                                                          |

## Public Victory (Collaboration)

| #   | Habit          | Dimension | Question                               | Result | Evidence                                                                                                          |
| --- | -------------- | --------- | -------------------------------------- | ------ | ----------------------------------------------------------------------------------------------------------------- |
| 9   | H4: Win-Win    | Heart     | Issue closures include rationale?      | Pass   | Each issue maps to a gap finding with acceptance criteria. Commits use Closes #N with context                     |
| 10  | H4: Win-Win    | Heart     | Error messages help next developer?    | N/A    | Plugin is markdown guidance — no runtime error messages. validate-structure.sh has clear PASS/FAIL output         |
| 11  | H5: Understand | Mind      | Read existing code before writing new? | Pass   | All 17 SKILL.md, all 8 habit files, SELF-CHECK, CLAUDE.md read before plan was designed                           |
| 12  | H5: Understand | Mind      | Reproduced bug first?                  | Pass   | Whole Person Assessment reproduced the gap honestly (3.0/5). Exploration confirmed specific missing sections      |
| 13  | H6: Synergize  | Heart     | Independent tasks running in parallel? | Pass   | Phases 1-3 are independent (different files). Research used 3 parallel agents                                     |
| 14  | H6: Synergize  | Mind      | Considered third alternative?          | Pass   | markdownlint rejected (npm dep), Thai expansion deferred (quality concern), external validation deferred (future) |

## Renewal & Significance

| #   | Habit           | Dimension | Question                     | Result | Evidence                                                                                                                                                                   |
| --- | --------------- | --------- | ---------------------------- | ------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 15  | H7: Sharpen Saw | Body      | Captured what was learned?   | Pass   | validate-structure.sh automates convention checks. feedback memories saved (always PR, always issues first)                                                                |
| 16  | H8: Voice       | Spirit    | Understand WHY this matters? | Pass   | WHY: previous self-assessment was dishonest (4.5/5). Heart/Spirit gaps mean the plugin teaches empathy and ethics but doesn't demonstrate them. Fixing this is H8 Modeling |
| 17  | H8: Voice       | Spirit    | Empowers next person?        | Pass   | CI validates structure for contributors. Before/after examples teach concretely. CONTRIBUTING.md + CI = contributor-friendly                                               |

## Score

**Pass: 16/17** | **N/A: 1** | **Adjusted: 16/16 = 100%**

**Band**: Well-prepared

## Dimension Summary

| Dimension         | Questions | Pass | Score |
| ----------------- | --------- | ---- | ----- |
| Body (Discipline) | Q1,2,3,15 | 4/4  | 100%  |

| Mind (Vision) | Q4,5,6,7,11,12,14 | 7/7 | 100% |
| Heart (Passion) | Q8,9,10,13 | 3/3 (+1 N/A) | 100% |
| Spirit (Conscience) | Q1,16,17 | 3/3 | 100% |

## Honesty Notes

v2.7.0 improvements (Reader Adoption — closing the /calibrate feature loop):

- **Hook-based verbosity adaptation** (#96) — `hooks/session-start.sh` reads `~/.claude/habit-profile.md` and emits a level-specific one-sentence directive into session context. Claude honors the directive when invoking any skill. Writer (v2.6.0) + reader (v2.7.0) = complete #90 feature loop.
- **`guides/verbosity-adaptation.md`** — canonical per-level rules + worked examples across 5 skill archetypes. Zero changes to individual skills.
- **`tests/test-verbosity-hook.sh`** — 11 new regression assertions covering 8 hook branches + HABIT_QUIET silence + token budget. Wired into CI as 4th validator. Validators grow 470 → 481.
- **Architectural forcing**: pre-implementation F3 word-budget audit revealed 2 skills with single-digit headroom, ruling out any per-skill approach and forcing the hook-based design. Demonstrates H3 (First Things First) — constraint surfaced before design saved a rework cycle.
- **Milestone v2.7.0 — Reader Adoption: CLOSED 1/1.**

v2.6.1 improvements (Skill Effectiveness Tracking + CI hygiene):

- **`/reflect` Q6 skill-effectiveness signal** (#92) — 6th question added asking "most useful / least useful or confusing skill this session". Feeds maintainer trend review.
- **`SKILL-EFFECTIVENESS.md`** (new, repo root) — maintainer-curated trend tracker with per-skill tally for all 17 skills; ships empty, populated from `/reflect` Q6 over time. H7 applied to the plugin itself.
- **SIGPIPE CI fix** — `tests/validate-content.sh` F3 convention-consistency check switched from `sed|head -20` to pipe-safe awk after PR #99 first CI run flaked on Linux. Root cause: `skills/ai-dev-log/SKILL.md` has a body `---` horizontal rule at line 65 making sed emit 187 lines from a 239-line file, overflowing `head -20`. BSD sed on macOS ignored SIGPIPE; GNU sed on Linux with pipefail exit 4. Same awk pattern as `tests/validate-structure.sh:27`.
- **Milestone v2.6.0 — Hermes-Inspired Improvements: CLOSED 5/5** with this release.

v2.6.0 improvements (Hermes-Inspired Improvements — 4 issues, 2 ADRs):

- **Persistent reflection artifacts** (#88) — `/reflect` now writes `~/.claude/lessons/YYYY-MM-DD-<slug>.md`. `/research` and `/build-brief` retrieve lessons before starting work. Closes the learning loop.
- **Habit nudge guidance document** (#89) — `guides/habit-nudges.md` specifies nudge catalog + trigger + cooldown semantics; runtime hook implementation deferred to `pitimon/claude-governance` per plugin boundary.
- **User Maturity Calibration** (#90) — `/calibrate` skill + `~/.claude/habit-profile.md` (schema v1, YAML frontmatter) + `guides/habit-profile-schema.md` public contract + **ADR-008** (5 interlocking design decisions: Alt E hybrid, dominant-level scoring, YAML frontmatter schema, standalone chain position, user-driven re-calibration).
- **agentskills.io NO-GO** (#91) — Deep + Compare research brief + hands-on sandbox test + **ADR-007**. Key insight: "the reach of a standard is measured by what adopting tools _parse_, not by their logo list."
- Validators grew 443 → 470.

v2.5.0 improvements (Testing & Discoverability):

- **`tests/test-skill-graph.sh`** (#79) — DAG validator for `prev-skill`/`next-skill` chains. 55 assertions. Wired into CI.
- **`hooks/pre-commit.sh.example`** (#80) — opt-in pre-commit template running `/review-ai` on staged files.
- **Bidirectional wiki ↔ skills linking** (#81) — each workflow skill links to its wiki page; validator enforces both directions.
- Validators: 443 total; CI runs 3 scripts.

v2.4.1 improvements (Honest Correction, 2026-04-09):

- **Deleted `/brainstorm`** — shipped prematurely in v2.4.0 without reading `superpowers:brainstorming` source. Same-day correction.
- **`HABIT_QUIET=1` opt-out** for the session-start hook — audience-honest acknowledgment that not every user wants the 7-step workflow reminder every session.
- **Core 5 framing** — `/requirements`, `/review-ai`, `/cross-verify`, `/research`, `/reflect` named as the 80% of daily work; other 11 skills as optional depth.
- **ADR-006**: Audience Honesty + Superpowers Deferral — the "read the peer plugin source before claiming parity" lesson.
- Modeling H5 (Understand first) by correcting a v2.4.0 mistake within 24 hours instead of defending it.

v2.4.0 improvements (/using-8-habits meta-skill):

- **`/using-8-habits`** — onboarding meta-skill explaining the 7-step workflow and decision tree for "which skill next?" Reduces 16-skill overwhelm for new users.
- Validator check 18: meta-skill content assertions (references every skill by name, has decision tree, etc.).

v2.3.0 improvements (EU AI Act Compliance Toolkit — Flagship feature):

- **`/eu-ai-act-check`** — 9-obligation tiered checklist for EU high-risk AI systems (Articles 9-15). Pre-flight scope check skips if not high-risk or not EU-targeted.
- **`docs/research/eu-ai-act-obligations.md`** — verified-quote research brief. _(Migrated to `claude-governance` v3.1.0+ in v2.14.2 per ADR-012; deleted from this plugin in commit `ed65b97`.)_
- **`guides/eu-ai-act-mapping.md`** — MUST/SHOULD/COULD tier mapping. _(Migrated; same reference.)_
- **`scripts/generate-ai-dev-log.sh`** — Article 11 transparency (AI-assisted dev log from git history).
- **ADR-005**: EU AI Act compliance toolkit scope + plugin-boundary decision (eventually migrating to `claude-governance`).
- Spirit dimension strengthened: compliance depth without enforcement theater.

v2.2.0 improvements (Body dimension level-up):

- **Content validation script**: `validate-content.sh` — 4 new check categories (section depth, markdown integrity, ADR format, handoff content)
- **Architecture fitness functions**: 3 tracked metrics — Skill Complexity Budget, Validation Coverage Ratio, Convention Consistency Score
- **Extended structure validation**: `allowed-tools` field validation, README ↔ skills cross-reference, agent definition checks
- **17 check categories, 215+ assertions** — up from 10/133 in v2.1.0
- **CI enforces both scripts** — fitness function breach fails the build
- **ADR-003**: Documents why bash content validation was chosen over npm linting or agent-driven validation
- **Body reaches Level 3**: Quality gates enforce standards automatically, fitness functions track architecture health

v2.1.0 improvements (Feynman-inspired multi-agent research):

- **Research depth levels**: `/research` now supports Quick/Standard/Deep — auto-detected from the argument or explicit keyword
- **Research modes**: General/Compare/Audit — comparison matrix and code-vs-docs audit output
- **`research-verifier` agent**: Source verification agent validates URLs, file paths, document references
- **Research brief template**: Structured output with optional comparison matrix, audit results, verification report
- **ADR-002**: Documents why modes integrate into `/research` rather than becoming separate skills

v2.0.0 improvements (ULW-inspired orchestration):

- **Orchestration classification**: `/breakdown` now classifies tasks as sequential/parallel-safe/parallel-worktree
- **Context boundaries**: `/build-brief` adds must-know/must-NOT-know/merge-contract per agent
- **Orchestration patterns guide**: 3-pattern catalog (Worktree Isolation, Context Boundaries, Meta-System Mindset)
- **Meta-system mindset**: H7 Rule 5 — "invest in the system that builds the system"
- **ADR-001**: First Architecture Decision Record — documents integrate-vs-new-skill decision
- **Inspiration**: UltraWorkers (ULW) multi-agent orchestration tools (OmC, OmX, clawhip)

v1.9.0 improvements (Feynman-inspired):

- **New skill**: `/research` (Step 0) — investigate before specifying requirements
- **Evidence grounding**: `/review-ai` now requires file:line evidence for every finding
- **Integrity principles**: 12 AI Integrity Commandments guide (Spirit dimension)
- **Confidence levels**: `/cross-verify` optional Verified/Inferred/Unverified marking
- **Lazy parallelism**: `/breakdown` + H6 gate — "Can I do this in ≤5 tool calls?"
- **Inspiration**: Feynman research agent patterns adapted for development workflow

The cross-verify score (16/16) measures **plan discipline**. The Whole Person score measures **plugin maturity**:

- v1.2.0: Body 2, Mind 4, Heart 3, Spirit 3 = **3.0** (inflated to 4.5 — dishonest)
- v1.6.0: Body 3, Mind 4, Heart 4, Spirit 4 = **3.75** (honest)
- v1.7.0: Body 4, Mind 4, Heart 5, Spirit 4 = **4.25**
- v1.8.0: Body 4, Mind 4, Heart 5, Spirit 4 = **4.25** (consolidated — discoverability, same scores)
- v1.9.0: Body 4, Mind 4.5, Heart 5, Spirit 4.5 = **4.5** (evidence grounding + integrity principles)
- v2.0.0: Body 4, Mind 5, Heart 5, Spirit 4.5 = **4.625** (orchestration patterns + meta-system mindset + ADR)
- v2.1.0: Body 4, Mind 5, Heart 5, Spirit 5 = **4.75** (research depth/modes + verification agent + evidence rigor)
- v2.2.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (content validation + fitness functions + CI enforcement)
- v2.3.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (EU AI Act compliance toolkit — Spirit depth, ADR-005)
- v2.4.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (`/using-8-habits` meta-skill + decision tree)
- v2.4.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (honest correction of /brainstorm ship-delete, HABIT_QUIET opt-out, ADR-006)
- v2.5.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (DAG validator + pre-commit template + wiki↔skills linking, +55 assertions → 443)
- v2.6.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Hermes-inspired arc: #88 persistent reflection, #89 nudge spec, #90 /calibrate + ADR-008, #91 agentskills NO-GO + ADR-007 — 443 → 470 assertions)
- v2.6.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#92 /reflect Q6 + SKILL-EFFECTIVENESS.md, SIGPIPE CI fix — milestone v2.6.0 CLOSED 5/5)
- v2.7.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#96 hook-based reader adoption — closes #90 feature loop, 470 → 481 assertions, zero per-skill changes)
- v2.7.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#110 /review-ai Performance axis + review-tests-first — minimal post-milestone refinement; 5 of 6 agent-skills candidates rejected in cost/benefit audit, honoring v2.7.0 "local maximum" framing)
- v2.8.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Claude Code Architecture Insights: #113 /reflect consolidation, #114 /build-brief compression awareness, #115 /breakdown fork agent pattern, #116 /design sticky latches — 4 skills enhanced with production patterns from "Claude Code from Source" analysis)
- v2.9.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Deep-Project Inspired Improvements: #118 interview protocol for /requirements, #119 workflow step awareness in session-start hook, #120 machine-readable structured output blocks — 491/491 assertions at release)
- v2.10.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Progressive-Disclosure SKILL.md Split: #125 ADR-009 convention, 3 largest skills refactored into SKILL.md + reference.md + examples.md triads, F6 sibling word-budget soft limit Check 9b added)
- v2.11.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Design Pipeline Completion + Wiki Redesign: #128 SKILL_OUTPUT:design closes last handoff gap, #127 20 wiki pages upgraded; 243 structure + 183 content assertions PASS)
- v2.11.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (CHANGELOG Drift Guard: #124 hardened Check 19 with 3 FAIL assertions, backfilled v2.9.0 + v2.11.0 entries — two-release drift pattern closed with fitness function)
- v2.12.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Code-Symbol Grep Evidence: #133 /research gains grep obligation for remove/dead/unused verdicts, research-verifier scope clarified with Limit of Verification + SEMANTIC-EVIDENCE-MISSING flag)
- v2.13.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Cross-Agent Discoverability: #135 skills/RESOLVER.md flat dispatcher, #136 llms.txt + AGENTS.md at repo root, #137 README "Thin Harness, Fat Skills" citation, ADR-010 + ADR-011, Check 20 + 21 added)
- v2.13.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (SELF-CHECK.md Body Freshness: #141 three-PR arc — #142 catch-up 6 missing per-release rows v2.9.0→v2.13.0 + footer, #143 CONTRIBUTING.md 3→4 files correction, #144 Check 19 sub-checks E + F — CI invariant via git tag source of truth)
- v2.14.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (TOH Framework Inspirations — milestone #15: #151 SKILL_OUTPUT attribution lines + Check 22, #149 argument-driven smart-routing in /using-8-habits, #150 Verification Phase in /review-ai with explicit "guidance only, NOT a hook" boundary qualifier — three workflow-discipline imports from external framework, all stayed within plugin boundary)
- v2.14.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#157 README "What's New" Drift Guard — external QA found Check 19A passes on badge URL not section header, README "What's New" missing v2.14.0 entry, TOC anchor broken since v2.3.0, architecture tree under-enumerated 4 skills; same bug class as #124/#141 recurring on sibling surface; fix is doc backfill + Check 19 sub-check G anchored grep + Check 20 hardening pinning Find→Fix→Re-Verify loop name and step count — capability-level pattern closed with fitness function not checklist)
- v2.14.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (EU AI Act Migration Completion — second half of cross-plugin migration coordinated with `pitimon/claude-governance` v3.1.0; deleted reference.md + research + mapping guide; rewrote SKILL.md as redirect stub preserving NOT LEGAL ADVICE disclaimer + ADR-012 reference; ADR-005 marked Superseded; Check 15 in validate-content.sh rewritten as stub-mode + negative-restore assertions; cross-refs reframed in RESOLVER, using-8-habits, design Step 5, ai-dev-log, session-start, README skill table; wiki + README badge deferred to follow-up doc-only PR per `pitimon/claude-governance` PR #25 + #26 formatter precedent — boundary correction completes the contract from memory observation #233270, plugin boundary integrity restored)
- v2.14.3: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#163 Post-Migration Cleanup + Validator Self-Discipline — ADR-012 metadata closure (SELF-CHECK lines 103-104 reframed + `**Implementation**:` field added to ADR-012 header naming commit ed65b97), `.gitignore` created with `/deep-project/` + `/.claude/` entries, `tests/validate-content.sh` trimmed 831 → 793 lines via comment consolidation across Check 15 + Check 19 sub-checks B/C/D/E/F/G + F2 + F3 — logic untouched, 10 checks preserved, PASS count 205 stable; closes credibility gap where validator violated 800-line rule it enforces on skills via Check 5 in validate-structure.sh)
- v2.15.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#165 Cross-Artifact Consistency Analyzer + Opt-In Spec Persistence — spec-kit `/analyze` inspiration, 18th skill `/consistency-check` (180 lines + reference 149 lines, 5 detection passes, hybrid eval), `--persist <slug>` flag added to `/requirements`+`/design`+`/breakdown` (back-compat invariant preserved), `guides/persistence-convention.md` single-source-of-truth, ADR-013 with 5 alternatives + flag precedent attestation (`/ai-dev-log`, `/calibrate`) + slug validation regex + hybrid pass strategy, dogfood at `docs/specs/consistency-check/{prd,design,tasks}.md`, validator `AskUserQuestion` whitelist + consistency-check TODO-pattern exemption with comment, plugin boundary preserved — read-only advisory analyzer, enforcement still belongs in claude-governance)
- v2.15.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#176 Current State Save-Point Convention — PR #177 convention-only addition to `guides/persistence-convention.md` importing community-article pattern (Thai-language article _"ผมไม่เคยกลัว /clear กับ /compact"_) as 4th user-owned file `docs/specs/<slug>/current-state.md`; solves resume-after-context-loss at task level — complements `hooks/session-start.sh:83-115`'s step-level artifact-detection (Issue #119); regeneration-safety rationale documented (separate file avoids ownership conflict with `/breakdown --persist X` Conflict policy on re-run); `/consistency-check` explicitly excluded (informal running state out of cross-artifact consistency scope); Auto-Update Recipe section provides CLAUDE.md rule template user adopts (plugin does NOT enforce — preserves ADR-013 Alt-4 no-build philosophy invariant); habit mapping H5+H3 for section, H1 for recipe; `@8-habit-reviewer` pre-commit review: 17/17 PASS + 2 polish items addressed in-PR (F1 ADR-013 Alt-4 quote accuracy "magic behavior" was Alt-2's wording, F2 frontmatter MUST scope ambiguity → explicit exemption added); guide grew 109→167 lines under 220 cap; pattern: community-article convention-only import distinct from v2.15.1's upstream-skill extract — article has no canonical PR/commit so paraphrased title + #176 issue link is maximum achievable attribution; honors PR #111's local-maximum lesson — no new skill/agent/validator/DAG)
- v2.21.19: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (AI Dev Log Snapshot and Trailer Fallback — fixes `/ai-dev-log` undercounting when Git's trailer formatter returns empty despite valid `Co-Authored-By:` lines in commit bodies; adds a recorded snapshot boundary plus `--snapshot <sha>` reproduction; keeps empty-state `--out` useful by writing a 0-detected report with limitations. Scope remains generator behavior only: no new skill, runtime enforcement, policy gate, hook behavior change, or diff/file-content inspection.)
- v2.21.20: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex SessionStart PATH Hardening — fixes startup hook exit 127 when Codex runs the 8-Habit `SessionStart` manifest with a stripped `PATH` that cannot resolve bare `bash` or common script utilities; manifests now seed a minimal system `PATH` and invoke `/bin/bash` explicitly. Scope remains hook manifest portability only: no new runtime enforcement, skill behavior change, policy gate, or Claude/Codex hook parity claim.)
- v2.21.33: Body 5, Mind 4, Heart 4.5, Spirit 5 = **4.6** (Blast-Radius Boundary-Crossing Flag in H1 — #327, sourced from a `/research deep` on `DeusData/codebase-memory-mcp` (a runtime SQLite code-graph MCP server). The tool was not adopted: a runtime server is out of the markdown boundary and its comprehension space is largely covered by the installed `claude-mem` suite (cf. #311). A claude-mem comparison isolated one genuine gap — `detect_changes`' architecture-boundary-crossing flag — extracted as one reflective H1 rule + a checkpoint clause. Spirit 5: a `@8-habit-reviewer` cross-verify (13/17) found ~2/3 of the original 3-file plan merely restated existing lines (H1 "trace ALL callers" :18, checkpoint :29, /diagnose :131/:141, /scrutinize ×5), and the scope was honestly collapsed to the single additive delta; phrased as a discipline question, not an enforcement gate, to respect the claude-governance boundary. Body 5: validators green, mirror byte-identical. Mind/Heart 4–4.5: one-clause addition to the highest-bar auto-loaded file, low new surface. Scope: rules/effective-development.md H1 + release surface.)
- v2.21.32: Body 5, Mind 4, Heart 4, Spirit 5 = **4.5** (Evenhandedness Steelman + Behavioral-Spec Craft + Staleness Honesty — #325, sourced from a `/research deep` comparison of a production AI behavioral system prompt against the plugin. Key judgment: the plugin had already independently converged on most such patterns, so only the three genuine gaps were adopted, and the steelman was folded into the existing Commandment 14 rather than minted as #15 to avoid cascading the "14 commandments" count across `/post-mortem` + `/review-ai` Load directives. Body 5: traced the word-count cap (F3, `/scrutinize` at 1972/2000) before editing and kept the weave minimal; mirror byte-identical; abstracted spec-craft techniques rather than republishing any vendor's prompt text. Spirit 5: evenhandedness is itself a conscience discipline (reject the real argument, not a strawman) and the staleness note extends knowledge-cutoff honesty. Mind/Heart 4: coherent extension of the adversarial + epistemic systems, low new surface. Scope: integrity-principles + 3 skill bodies + 1 new guide + 2 Load refs + release surface. D/E deferred, F → claude-governance per boundary.)
- v2.21.31: Body 4, Mind 4, Heart 5, Spirit 5 = **4.5** (Windows Git Bash Preflight for Codex Validation — #323, surfaced by live test-station validation on Windows PowerShell. The key judgment was rejecting parallel PowerShell validator ports and instead adding one preflight script that checks `codex`, `git`, `node`, Git Bash, installed plugin listing, and the bare-`bash` WSL launcher collision. Heart 5: reduces client confusion with copy-pasteable Git Bash commands while preserving the repo's single Bash validator source of truth. Spirit 5: honest platform contract — Windows support means Git Bash compatibility, not hidden PowerShell-native parity. Body/Mind 4: small script + docs + release surface, no new runtime capability or enforcement.)
- v2.21.30: Body 5, Mind 4, Heart 4, Spirit 5 = **4.5** (Codex Hook-Config Schema Purity + Doctrine Reconcile — #321, surfaced by the maintainer's own Codex install. Root cause via `/research` Audit: Codex auto-parses `hooks/hooks.json` at install with a strict serde schema (top level = `hooks` only), so the top-level `description` field triggered `unknown field 'description', expected 'hooks'`. Body 5: didn't just delete the field — added validator Check 31 as a forcing function so the class can't recur, and reconciled 3 doctrine docs (`.codex/README.md`, `compatibility-matrix.md`, `codex-integration.md`) that conflated _parse_ with _execute_ and contradicted both each other and `session-start.sh`'s live Codex-aware code. Spirit 5: chose the root-cause fix (option C) over the 2-line patch; documented the real contract honestly rather than restating the false "Codex does not run hooks" line. Mind/Heart 4: config + doc + validator surface, no new capability class. Scope: 2× hooks.json + Check 31 + 3 doctrine docs (×2 mirrors) + release surface. Caught the recurring Codex-hook-surface pattern (>=4 prior fixes: emit JSON, read manifest, PATH hardening, now schema purity).)
- v2.21.29: Body 5, Mind 4, Heart 4, Spirit 5 = **4.5** (Trap-Naming Taxonomy Lens — #319, inspired by the `/research` brief on UditAkhourii/adhd. Added a 14th integrity commandment "Never let a seductive-but-broken option pass unnamed" + 4-mode failure taxonomy (hidden cost / false economy / scaling failure / premature abstraction) to `guides/integrity-principles.md`, and wired it into `/scrutinize` Step 1 (tag rejected alternatives) and `/cross-verify` Shadow Self-Check (test the recommendation itself). Body 5: full research→requirements→design→breakdown→impl trail, all 3 validators green, mirror byte-identical, and traced all callers — caught + fixed the "(the 13 commandments)" count refs in `/post-mortem` and `/review-ai` that the breakdown missed. Spirit 5: the commandment is itself a conscience discipline (don't fool yourself with a seductive option); chose Option B (extend existing guide both skills already Load) over net-new surface; SKILL-EFFECTIVENESS pulse note instead of a false-claim tally bump. Mind/Heart 4: coherent extension of the adversarial system, low-surface craft, no new capability class. Scope: integrity-principles guide + 2 skill bodies (+2 count-ref fixes) + release surface.)
- v2.21.28: Body 5, Mind 4, Heart 4, Spirit 4 = **4.25** (Post-release QA polish — #316 + #317, found by the maintainer's own Codex-install QA of v2.21.27. #317: README "What's New" TOC anchor had silently drifted to v2.21.12 for ~15 releases; fixed it AND added a `validate-content.sh` Check 19 sub-check that derives the slug from `plugin.json` so the class can't recur — Body 5 for closing the recurring drift with a forcing function, not just the instance. #316: regrouped `/deploy-guide` DoD 10→3 grouped checkboxes with sub-items preserving every gate + Check 23 phrases, clearing the density warning. Mind/Heart/Spirit 4: small readability + validator-coverage polish, no new capability. Scope: deploy-guide DoD + README + validate-content.sh Check 19 + release surface.)
- v2.21.27: Body 4, Mind 5, Heart 4, Spirit 5 = **4.5** (Invocation-Aware SKILL-EFFECTIVENESS + Usage≠Revocation Principle — #314. A user challenge ("is usage-history-as-revocation-criterion correct?") prompted a 3-layer audit (lessons Q6 + memforge 121k obs + 2,534 transcripts/53 projects) that overturned the "14 zero-signal = unused" reading: 5 of the 14 had real external invocation (`requirements` 14×, `ai-dev-log` on sysAdmin2025, etc.) — Q6 is blind to Skill-tool/script/cross-project runs. Codified a Governing principle: usage informs discoverability + footprint only, never revocation. Added a transcript-invocation harvest step (with observer-pipeline + dogfooding discount rules) + a per-skill Skill class index. Mind 5: caught the metric-becoming-policy drift before it hardened. Spirit 5: human-owned existence decision made explicit; chose instrumentation over pruning. Heart/Body 4: doc + protocol surface, no hook/validator behavior change. Scope: SKILL-EFFECTIVENESS.md + CONTRIBUTING line + release surface.)
- v2.21.26: Body 4, Mind 5, Heart 4, Spirit 5 = **4.5** (Codebase-Orientation Skill — Out-of-Scope Record — closes #311 as T3 (ADR-018 ship-decision table) with a `docs/out-of-scope/codebase-orientation-skill.md` rationale record instead of shipping a 25th skill. The deferred Idea A from the Understand-Anything research (v2.21.24) was evaluated via the ADR-018 `predicted_uses` contract and rejected: redundant with the installed `claude-mem` comprehension suite (pathfinder/learn-codebase/smart-explore) + low predicted-use frequency + the only distinct angle (H5 discipline) fits a guide paragraph not a skill. Mind 5: a documented "why not" decision with reconsideration conditions is the higher-leverage artifact than the skill itself. Spirit 5: chose to shrink-not-grow surface against the temptation to ship a plausible feature. Scope: out-of-scope doc + release surface only, no skill/hook/validator behavior change.)
- v2.21.25: Body 4, Mind 4, Heart 4, Spirit 5 = **4.25** (SKILL-EFFECTIVENESS Tally Harvest + Anti-Dormancy Gap Close — closes a 6-release dormancy gap (v2.21.19–v2.21.24 shipped with no SKILL-EFFECTIVENESS pulse note, past the ≥2-cycle ADR-018 reversal threshold), caught by an end-of-session "anything left?" sweep. Honest harvest of 3 accumulated lessons: +3 `/cross-verify` (21→24), +1 `/diagnose` missed_skill (0→1, first non-zero entry in that column — validates the v2.19.2 field split). Spirit 5: the forcing function caught its own dormancy and the harvest is grep-evidenced, not a false-claim bump. Body/Mind/Heart 4: mechanical tally + release surface, no new capability. Scope: maintainer-curated report + release docs only, no skill/hook/validator behavior change.)
- v2.21.24: Body 4.5, Mind 4.5, Heart 4, Spirit 4.5 = **4.4** (Cross-Platform Frontmatter Rule + Disk-Intermediate Orchestration Precedent — two engineering lessons mined from `Egonex-AI/Understand-Anything` (58k★) via `/research deep`, folded into existing guides with **no new skill surface** (ADR-018 "earn each line"). `guides/vendor-portability.md` gains a rule banning the Claude-only `model: inherit` keyword (opencode rejects it as `ProviderModelNotFoundError`, their #167) while preserving deliberate documented model pins — our reviewer agents stay `model: opus` per #285. `guides/orchestration-patterns.md` Pattern 2 gains a disk-intermediate context-economy precedent. Spirit 4.5: the `8-habit-reviewer` cross-verify caught that the naive "never hardcode model" rule would have silently contradicted #285 — corrected before ship. A third idea (codebase-orientation `/research` mode) deferred as a documented gap. Scope: guide content only, no skill/hook/validator behavior change.)
- v2.21.23: Body 5, Mind 4, Heart 4.5, Spirit 4 = **4.4** (Diagnose Load Path Portability Fix — resolves review blocker F1 (issue #308): `/diagnose` Load directives were absolute maintainer-machine cache paths pinned to v2.18.0, silently broken for every other installer since the skill shipped; now `${CLAUDE_PLUGIN_ROOT}`-relative. Adds Check 8b class guard failing any absolute-path Load directive, negative-tested with a synthetic injection. Body 5 for the guard + negative test; Spirit 4 not 5 because the defect shipped unnoticed for ~3 weeks and was caught by review, not by the validator suite; Mind 4 because review findings F3–F21 remain open.)
- v2.21.22: Body 4, Mind 3.5, Heart 4, Spirit 3 = **3.6** (Fable Model Review Record — adds `docs/reviews/2026-06-10-fable-model-review.md`, a five-pass 8-Habit review of v2.21.21 scoring 13/17 with verdict fix-then-ship. This entry adopts the review's independent dimension scores instead of a self-assessed 5.0 — the first sub-5.0 entry since v2.2.0 — in direct response to the review's F4 grade-saturation finding: a self-check that records perfect scores for ~60 consecutive releases has stopped discriminating. Headline findings: F1 `/diagnose` Load directives hardcode an absolute maintainer path pinned to v2.18.0 (blocker, broken for all installers); F3 CLAUDE.md skill table stale at 19/24; F21 Check 27/28 catch-22 makes ADR-019's docs-only bump exemption unreachable — this very release reproduced it. Scope: documentation only, no skill/hook/validator behavior change.)
- v2.21.21: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (CI/CD Proof-Scope Discipline — adds `/deploy-guide` proof layers for configuration, workflow, runner identity, runtime, and release validation; warns that old tag reruns may not include current-main workflow fixes; requires self-hosted runner identity evidence and `Refs` instead of `Closes` while final live proof is pending. Scope remains markdown guidance and validator pins only: no runner automation, issue mutation, deployment engine, or new operational-state skill.)
- v2.21.18: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Epistemic Footer and Shadow Self-Check — makes `/cross-verify` confidence reporting required for high-stakes reviews, adds open unknowns, extends the research brief template with Confidence & Open Unknowns, aligns integrity principle #7 to the shared `V/I/U` vocabulary, and centralizes the Shadow Self-Check in `/cross-verify` instead of duplicating it into `/scrutinize`. Scope remains markdown guidance only: no runtime enforcement, hook behavior change, new skill, orchestration engine, policy gate, or hook parity claim.)
- v2.21.17: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Software Ecology Guardrail — adds lightweight AI acceleration impact checks to `/research`, `/design`, and `/review-ai` for review load, validation cost, handoff drift, internal contracts, human attention, validator brittleness, generated-content noise, and contract drift. Scope remains markdown guidance only: no runtime enforcement, hook behavior change, orchestration engine, policy gate, or hook parity claim.)
- v2.21.16: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Requirements Intake Mode Split — adds Existing-system mode and Idea-mode to `/requirements`, with evidence citation for real systems and assumption labeling for raw ideas. Step 1 wiki and validator pins keep the mode split discoverable and regression-checked. Scope remains markdown guidance only: no new skill, runtime enforcement, template suite, `/build-brief` handoff change, or Claude-only behavior.)
- v2.21.15: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (SessionStart Dead Variable Cleanup — removes unused `BRIEF` and `PROGRESS` assignments from `hooks/session-start.sh` while preserving existing inline workflow artifact markers and SessionStart behavior. Scope is cleanup only: no new hook content, runtime enforcement, policy gate, dynamic orchestration, or broad Claude-hook feature parity claim.)
- v2.21.14: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex SessionStart Version Fallback — teaches `hooks/session-start.sh` to read version from `.claude-plugin/plugin.json` first and `.codex-plugin/plugin.json` second, preventing `vunknown` in Codex package roots. Adds regression coverage using a fake Codex-only package root. Scope is version rendering only: no new hook content, runtime enforcement, policy gate, dynamic orchestration, or broad Claude-hook feature parity claim.)
- v2.21.13: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (SessionStart Root Fallback — fixes the installed hook command path when `CLAUDE_PLUGIN_ROOT` is unset by falling back to `CODEX_MANAGED_PACKAGE_ROOT` and then package-root cwd. Adds regression coverage that executes the registered `hooks/hooks.json` command with `CLAUDE_PLUGIN_ROOT` unset. Scope is path compatibility only: no new reminder content, runtime enforcement, policy gate, dynamic orchestration, or broad Claude-hook feature parity claim.)
- v2.21.12: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (QA Polish and Design Wiki Example — adds a concrete Step 2 Design wiki example for pass level, claim labels, evidence strength, `Verify first: Yes/No`, question priority, and Mermaid traceability. Compacts `/diagnose` and `/research` Definition of Done lists to remove existing validator warnings while preserving their behavioral requirements. Splits `/ai-dev-log` script internals and report-template detail into `reference.md` to remove the remaining skill-complexity fitness warning. Scope is polish only: no new skill, runtime enforcement, hook behavior change, or semantic change to `/diagnose`, `/research`, or `/ai-dev-log`.)
- v2.21.11: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Design Architecture Claim Discipline — improves `/design` with `Scan`, `Focus`, and `Full` pass levels, promotion only with evidence, explicit user request, or risk of staying smaller, and load-bearing claim labels with evidence strength plus `Verify first: Yes/No`. ADR template gains optional Architecture Claims and Decisions Requiring Approval sections. Scope remains portable markdown guidance adapted from `aetox-skills/senior-architect-agent`: no new senior-architect skill, full template suite, runtime enforcement, policy authorization, or Claude-only behavior.)
- v2.21.10: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Linux Claude Code Install Compatibility — replaces the root `plugin -> .` symlink with a real `plugin/` Codex child-source package after Linux Claude Code install failed with `ENOENT: no such file or directory, symlink`. Adds `plugin/.codex-plugin/plugin.json` plus packaged `plugin/skills/`, extends Check 28 to reject root symlinks, require child-package skills, and detect root-vs-child package drift, preserving Codex child-source install behavior. Scope is packaging compatibility only: no skill behavior change, runtime enforcement, policy authorization, or Claude hook parity claim.)
- v2.21.9: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex Skill Invocation Guidance — clarifies that Claude Code slash notation such as `/cross-verify` is not a plugin-provided top-level slash command in Codex. README, AGENTS.md, `docs/codex-integration.md`, and `docs/compatibility-matrix.md` now direct Codex users to `/skills`, `$skill-name` mentions, app plugin/skill mentions, or natural-language intent. Scope is documentation only: no custom prompt workaround, runtime dispatcher, hook parity claim, or skill behavior change.)
- v2.21.8: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Opus Reviewer Agents — moves the two read-only Claude Code reviewer agents, `8-habit-reviewer` and `research-verifier`, from Sonnet to Opus for high-stakes cross-verification and citation-integrity gates. README and compatibility docs keep the boundary explicit: this is Claude Code agent model selection only; Codex continues to consume the shared markdown skills and does not gain Claude subagent model parity.)
- v2.21.7: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Issue Tracking and Tracer-Bullet Planning — adds issue pickup/progress/completion comment templates, context-aware requirements grilling, vertical-slice breakdown wording, and a TDD tracer-bullet guide. Bundles post-v2.21.6 maintenance: Codex update-flow clarification, link-check exclusion hardening, and Node 24-compatible checkout workflows. The release converts the next-round `mattpocock/skills` research into portable markdown discipline while preserving boundaries: no automatic issue mutation, no setup engine, no test runner, no runtime enforcement, and no dynamic orchestration.)
- v2.21.6: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex SessionStart JSON Compatibility — fixes #284 by wrapping the existing `hooks/session-start.sh` markdown reminder in Codex-compatible `hookSpecificOutput.additionalContext` JSON when Codex runtime variables are present, while preserving Claude/default markdown output and `HABIT_QUIET=1`. Adds `HABIT_HOOK_OUTPUT=codex-json|markdown` smoke-test overrides, updates compatibility docs and generated catalog wording, and expands `tests/test-verbosity-hook.sh` with a JSON parse regression. Scope is a narrow hook-output adapter only: no runtime enforcement, no policy authorization, no dynamic orchestration, and no general Claude hook feature parity.)
- v2.21.5: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Project Context Contract — adds `guides/project-context-contract.md` and optional context awareness across `/requirements`, `/design`, `/breakdown`, `/build-brief`, `/diagnose`, and `/scrutinize`. Imports the useful part of the mattpocock setup/context pattern while preserving this plugin's boundary: markdown guidance only, no setup engine, no automatic issue mutation, no runtime enforcement, and no Claude hook port to Codex.)
- v2.21.4: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Release Decision Gate — improves `/deploy-guide` and contributor release discipline so PRs classify `Release now`, `Bundle later`, or `No release` before any version bump. Adds a validator pin in `tests/validate-content.sh` so the gate keeps all three categories and the invariant: if no release is intended, do not bump version files. Scope remains markdown workflow guidance and validation only: no runtime enforcement, no marketplace behavior change, and no Claude hook port to Codex.)
- v2.21.3: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex Project Context Files — adds `DOMAIN.md`, `SPEC.md`, `.codex/README.md`, `docs/adr/README.md`, and ADR-025 so Codex and other non-Claude agents have concise repo-local entry context without overwriting `CLAUDE.md`. `AGENTS.md` now points at the context files and `llms.txt` indexes them for remote discovery. Scope remains documentation and session ergonomics only: no skill behavior change, no runtime enforcement, no marketplace behavior change, and no Claude hook port to Codex.)
- v2.21.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex Installed-Cache Validator Context — fixes #278 by making validate-structure Check 28 distinguish source/marketplace publishability from Codex installed-cache shape. Source and marketplace snapshots still require `plugin -> .`; installed cache roots under `~/.codex/plugins/cache/...` may omit that symlink because the installed root is already the plugin root. Docs now explain the two validation contexts. Scope remains packaging validation and documentation only: no skill behavior change, no runtime enforcement, no marketplace behavior change, and no Claude hook port to Codex.)
- v2.21.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Cross-Agent Evidence Discipline — adds wiki Limitations page, expands runtime/plugin comparison and release-evidence guidance in `docs/compatibility-matrix.md`, adds real-behavior-proof expectations to `CONTRIBUTING.md`, links the new boundary page from README/wiki/llms.txt, and makes `docs/wiki/Limitations.md` part of the required wiki skeleton. Release checklist also harvested one previously untallied `/whole-person-check` skill-effectiveness signal and aligned the report with all 24 current skills. Scope remains documentation and release discipline only: no package behavior change, no runtime enforcement, no marketplace behavior change, and no Claude hook port to Codex.)
- v2.21.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Cross-Agent Discovery and Portability Contract — adds generated `docs/data/skills.json` from `skills/*/SKILL.md`, dependency-free Node generator `scripts/generate-skill-catalog.js`, and validate-structure Check 30 freshness validation. Documents the `SKILL.md` frontmatter compatibility contract in `CONTRIBUTING.md`, `guides/skill-authoring.md`, and `docs/compatibility-matrix.md`; links catalog discovery from `llms.txt` and `docs/codex-integration.md`; adds handoff-integrity guidance to `guides/structured-output-protocol.md`; and adds observable AI-work health signals to `/review-ai`, `/reflect`, and `guides/quick-reference.md`. Scope remains markdown guidance and generated metadata only: no runtime dispatcher, no Claude-hook port to Codex, no budget enforcement, no policy gate, and no agent-to-agent orchestration protocol. Host portability explicitly covers macOS, Linux, and WSL.)
- v2.20.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Production Canary Reconciliation Gates — extends existing `/deploy-guide` to close #250 without adding runtime automation. Adds `production canary / capacity change` deploy type and a provider-managed canary template covering precheck, cordon approval, observation, drain approval, provider-side change approval, reconciliation, and postcheck/evidence closure. The reconciliation gate explicitly handles provider-selected target drift, distinguishes provider scale/update success from full canary reconciliation success, checks no unintended `SchedulingDisabled` nodes remain, and requires pre-scale rollback/mitigation plus post-scale mitigation readiness. Evidence wording records intended canary, provider-selected target, scale/update status, reconciliation status, mitigation readiness, and `/operational-state` handoff. Scope remains markdown guidance only: no cloud execution, Kubernetes/ASG automation, policy enforcement, or runtime state engine.)
- v2.20.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Incident/Config Consistency-Lite — extends existing `/consistency-check` instead of adding a new skill, closing #253 after `/operational-state` supplied the state vocabulary. The new mode works without persisted `docs/specs/<slug>/` artifacts and emits `symptom | evidence | root cause | fix | verification | drift` for operational hotfix PRs, alert/config changes, changelog entries, deploy notes, and closure text. It flags missing evidence, overclaiming PR/changelog text, fix-scope mismatch, deploy-path drift, and adjacent operational state left unclassified; unresolved related state hands off to `/operational-state`. Reference includes a generic WorkerDown/Alertmanager-style example. Scope remains markdown guidance only: no runtime enforcement, no issue/alert mutation, no cloud execution, no new skill inventory.)
- v2.20.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Operational State Model — adds new read-only `/operational-state` skill closing #251. The skill classifies operational findings as Watch, Fix Candidate, Active Incident, Resolved, Handoff, Known Accepted Issue, False Positive, or Self-Resolved before action; maps each state to evidence, allowed/prohibited actions, human approval, artifacts, escalation criteria, and closure criteria; and explicitly prevents the observed false closures "Running equals healthy" and "recovered equals fixed". Scope stays markdown guidance only: no runtime state engine, policy enforcement, cloud execution, alert suppression automation, or automatic production writes. #250 production canary workflow and #253 incident/config consistency-lite remain follow-on work using this vocabulary. Minor bump because a new consumer-facing skill changes the plugin inventory from 23 to 24; README, wiki, RESOLVER, using-8-habits, llms.txt, Claude/Codex manifests, CHANGELOG, and SELF-CHECK are version-synced.)
- v2.19.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Operational Doctrine Patch from Open Issues — closes the low-risk slice of #252/#254/#255/#256 across existing skills only; `/deploy-guide` classifies deploy type before rollout planning and narrows config-only skip semantics, `/security-check` expands infra/config/notification trigger coverage, `/reflect` preserves six questions while splitting Q6 into most/least/missed-skill fields, and `/management-talk` adds channel-shaped operational incident closure examples; broader operational-state work #250/#251/#253 deferred; no runtime automation, enforcement, cloud execution, or new operational-state skill added)
- v2.19.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Codex Runtime Compatibility Contract — adds `docs/compatibility-matrix.md`, `docs/codex-integration.md`, ADR-024, and validator Check 29; clarifies Codex installs the same 23 markdown skills but Claude hooks are not Codex runtime behavior; no skill rewrite, no runtime enforcement; future Codex adapter is bounded to routing, validation, release reconciliation, and curated memory deposit; README/AGENTS/llms entrypoints updated)
- v2.19.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Native Codex Plugin Packaging — adds `.codex-plugin/plugin.json` and `.agents/plugins/marketplace.json` so Codex can install the same 23 markdown skills through its native marketplace flow; ADR-023 records the boundary: no runtime enforcement, no skill rewrite, Claude hooks remain Claude-only; `tests/validate-structure.sh` now validates Codex packaging and treats Codex packaging paths as consumer-doctrine; version convention expands from 4 to 5 files; H1 proactive packaging guard, H4 install-path deposit for Codex users, H5 preserves existing source-of-truth skills, H8 honest cross-agent promise made executable)
- v2.18.9: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Diagnose Worked-Example Polish — craft follow-up to v2.18.8 closing the Heart-dimension gap a `/whole-person-check` flagged (the v2.18.8 Phase 4 step told the reader _what_ to do but the copy-pasteable _how_ lived one click away in `guides/independent-source-verification.md`); adds a runnable one-liner to `skills/diagnose` Phase 4 — `a=$(docker run --rm … grep VERSION)` vs `b=$(docker exec … grep VERSION)` then `[ "$a" = "$b" ] || echo DIVERGENCE` — plus a one-line generalization ("read the claim twice from sources that can't share the same mistake; believe it only when they agree") so the technique transfers to compile-from-source-vs-installed-package, DB-row-vs-API-response, two-log-vantages; boundary self-check: 0 agent-orchestration terms (`parallel()`/`spawn`/`orchestrate`) — independence-of-source stays the rule, ADR-021 engine boundary intact; diagnose 1493→1585 words (F3 cap 2000 safe); Gap 2 AC preserved (independent/different-tool/reconcile still grep-present); H4 Win-Win (lowers the DX barrier for the next first-time `/diagnose` user — empathetic worked example, not just a rule) + H8 Heart (deliberate honest 4→5 close: the `/whole-person-check` resisted score inflation by naming the one real seam, then this PR fixed exactly that seam); consumer-doctrine bump per ADR-019 (`skills/**` MUST bump) atomic across 4 files; validator state: structure + content ALL PASS / 0 breaches; PR closes the whole-person Heart follow-up)
- v2.18.8: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Independent-Source Verification — closes #243; a follow-up to #241/ADR-021 adding the **discipline** (not the engine) exposed by the first real fan-out use: a confident-but-wrong root cause read from a single tool (`docker exec` → "stale config baked by buildx cache") survived `advisor`, a `governance-reviewer` pass, a merged PR, AND a written `/reflect` lesson — every author-side gate reused the same contaminated observation, so none could diverge; an independent probe (`docker run <img>` = correct version) contradicted it and surfaced the real cause (worker bind-mounts host `scripts/` over `/app`, deploy bumped `IMAGE_TAG` but skipped the script rsync — the image was correct all along); transferable rule: a root cause from a single observation source is unverified — confirm from an _independent_ source (different tool/command/vantage) and reconcile contradictions rather than pick; **Gap 0** new canonical `guides/independent-source-verification.md` (case study + reusable tell `docker run <img>` ≠ `docker exec <container>` → a mount overrides the image + the MQIPT OCR-misread second instance proving the discipline generalizes beyond docker) cited by all six gap edits so they share ONE stable source not drifting inline copies; **Gaps 1-6** = `orchestration-patterns.md` Pattern 4 diagnostic-escalation trigger, `diagnose` Phase 4 independent-source paragraph + DoD item, `cross-verify` Q12 extension + diagnosis-reconciliation callout (folded into existing Q12 rather than renumber the canonical 17 — avoids cascade into scoring bands + README + llms.txt), `post-mortem` §7 "overturned root cause" reason, `reflect` Q2 "did a prior diagnosis prove wrong?" sub-prompt, `advisor-pattern.md` "is all evidence single-source?" check for the gate that actually failed here; **boundary review** — `8-habit-reviewer` (disprove-mode, single ADR-021 question) caught one DRIFT (Pattern 4 heading "a reason to fan out" + imperative "fan out diagnostic probes" re-owned the engine inside a guide that defines fan-out as spawning parallel sub-agents — the same #241-class confusion) → reworded so independence-of-source is the rule and fan-out is one optional delivery mechanism → re-checked CLEAN; **self-application** — Gap 4 tipped `post-mortem` past the 2000-word convention-consistency fitness cap (1990→2026), so the addition was merged into the adjacent "Incomplete prior fix" bullet (1999 words) rather than padding the skill — the fitness function working as designed; H1 Be Proactive (the discipline prevents shipping a wrong fix) + H4 Win-Win (the failed-gate analysis is a deposit for the next debugger) + H5 Seek First to Understand (reproduced the coverage gap via 6 grep ACs at 0 AND a concept-level cross-check before writing) + H7 Sharpen the Saw (the lived misdiagnosis becomes a reusable rule); validator state: `validate-structure.sh` ALL PASS / 0 FAIL, `validate-content.sh` ALL PASS / 0 fitness breaches (convention-consistency restored to 100% after the post-mortem trim); plugin boundary respected — discipline only, the fan-out engine stays in `claude-governance` per ADR-021; PR closes #243)
- v2.18.7: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Dynamic Workflow Positioning — closes #241; positions the plugin against Opus 4.8's new dynamic-workflow engine (deterministic scripts spawning `parallel()`/`pipeline()` sub-agents) which collides by name + philosophy with the plugin's human-gated `/workflow` skill; 4-probe parallel audit (skills/`/workflow`, autonomy ADRs 005/006/008/012/015/018, plugin boundary, habit rules) established the layering; **container decision** — extend existing `guides/orchestration-patterns.md` with Pattern 4: Fan-Out Discipline rather than create a new guide (advisor pressure-test reversed the initial new-guide instinct: two guides covering multi-agent orchestration is the fragmentation the "don't duplicate" rule warns against, same scope-creep shape `8-habit-reviewer` flagged in the ADR-020 arc); Pattern 4 = when/when-not-to-fan-out gate keyed to habits (H6 Synergize REINFORCES "use parallel agents for independent tasks"; H1 trace-all-callers / H3 one-thing-at-a-time / H5 read-before-write / H8 own-the-why all in TENSION with autonomous fan-out) + Article 14 oversight-under-fan-out checklist (preserve Understand/Override/Stop) + Quick Reference row, delegating the _how_ to existing Patterns 1-3 (no duplication); ADR-021 owns disambiguation + boundary positioning, quoting the CLAUDE.md rule-of-thumb verbatim so the ADR does not over-claim — the **engine** (runtime + agent-spawn authorization) belongs to `claude-governance`, only the fan-out **discipline** lives here; H1 Be Proactive (document the boundary before a future engine-wrapping skill violates it) + H5 Seek First to Understand (4-probe audit + read orchestration-patterns.md before writing, found the existing guide) + H6 Synergize (used the Opus 4.8 engine itself to run the audit) + H8 Find Your Voice (architecture decision is human-owned; boundary verdict quoted not invented); H8 Modeling — the new capability is referenced never wrapped, preserving guidance-only charter (CLAUDE.md §Architecture); Forward-Guardrail framing honest — zero first-person friction signal (collision is structural, surfaced by audit not by a `/reflect` lesson), Sunset 2026-11-24 with reversal criterion per ADR-014/017/020 precedent; consumer-doctrine bump per ADR-019 (`guides/**` MUST bump) atomic across 4 files; plugin boundary respected per memory obs #233270 — no validator extension, no PreToolUse hook; PR closes #241)
- v2.18.6: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Step 4a awk Made Frontmatter-Aware — fast-follow fix to v2.18.5 closing #239 caught ~1h after release in a QA pass; step 4a prescribed `awk '/^---$/{c++; next} c>=2'` which returned 0 for files without YAML frontmatter (ADRs and guides — 2 of 3 artifact types step 4a step 1 names), directly contradicting the release's own ADR-017 ~150-line case study; receipts: ADR-017 returned 0, ADR-018 returned 2, `guides/cross-verification.md` returned 0, `skills/requirements/SKILL.md` returned 131 (the only correct case); a contributor following 4a literally on an ADR precedent would have measured 0 → set FR ceiling at 0 × 1.20 = 0, defeating the calibration; fix: `skills/requirements/SKILL.md:80` now uses `awk 'NR==1 && $0=="---"{f=1; next} f && $0=="---"{f=0; next} !f'` which strips frontmatter only when the file actually starts with `---`; verified ADR-017 → 152 ✓, ADR-018 → 145 ✓, ADR-016 → 205 ✓ (handles ≥2 mid-body thematic breaks correctly), `cross-verification.md` → 95 ✓, `requirements/SKILL.md` → 131 ✓ (frontmatter still stripped); `tests/validate-content.sh` Check 22 closes the regression-gap #239 explicitly named ("`tests/**` was untouched, so no validator caught it") with 7 new PASS assertions (5 receipts including ADR-016 to lock in the "mid-body `---` thematic breaks are body" behavior + 2 grep assertions) — grep-asserts SKILL.md carries new awk variant AND is free of broken v2.18.5 form (positive + negative regression guard); runs prescribed awk against 4 representative files spanning all 3 artifact types and asserts body counts match expected; `guides/templates/prd-template.md` calibrated example updated from "~150" approximation to exact 152-line measurement, with one-line note clarifying ADR/guide precedents have no frontmatter (body count = `wc -l`); H1 Be Proactive (regression test moves catch from runtime to CI) + H4 Win-Win (deposit comment on #239 includes full receipts table + lesson framing) + H5 Seek First to Understand (reproduced issue receipts on actual files before touching code) + H7 Sharpen the Saw (Check 22 establishes precedent that future awk/grep/jq snippets in skill prose ship with paired assertion — H7 applied to the meta-tool); H8 Modeling — v2.18.5 step 4a is itself an instance of "test the actual outcome, not just process exit" (rules/coding-style.md anti-pattern), and shipping the regression-gap-closer alongside the bug fix demonstrates the discipline applies to its own meta-tool; consumer-doctrine bump per ADR-019 (`skills/**` + `guides/**` MUST bump, `tests/**` is contributor-doctrine no-bump but rides in the same PR); validator state: validate-structure.sh 358 PASS / 0 FAIL preserved, validate-content.sh adds Check 22 with 6 new PASS assertions; pattern: **prescribed command in skill prose = production guidance**, the structural validators check shape not snippet-correctness, so every `awk`/`grep`/`jq` snippet earns a paired assertion at ship-time not at the next bug-report-time; lesson on whether unit tests are standard for this plugin — for markdown-only plugins the test surface is structural validators not pytest, but the regression-gap discipline is identical; Forward-Guardrail Sunset 2026-11-24 inherits from v2.18.5; PR closes #239)
- v2.18.5: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (PRD Calibration Checkpoint — closes #237 action item from lesson `2026-05-24-v218-4-skill-authoring-double-rescue.md` §5; adds `4a. Calibrate numeric ceilings against precedent` sub-step to `skills/requirements/SKILL.md` Process step 4 (7 lines added, well within the issue's ≤15-line budget) covering 4-step authoring discipline — (1) identify closest precedent in repo by artifact type (`docs/adr/ADR-0*.md` / `guides/*.md` / `skills/*/SKILL.md`), (2) measure body excluding YAML frontmatter via `awk '/^---$/{c++; next} c>=2' <file> | wc -l`, (3) set FR ceiling at `precedent_max × 1.20` because aspirational round numbers contaminate `/consistency-check` runs once the artifact lands at its actual required size, (4) opt-out with one-line FR-comment rationale when no precedent exists or cap set by different constraint (hook token budget, validator string limit); `guides/templates/prd-template.md` Success Criteria section gains calibrated-vs-uncalibrated FR-007 example showing `≤180 lines (+20% from ADR-017 ~150)` versus `≤50 lines`; case study: v2.18.4 FR-007 in `docs/specs/skill-authoring-guide-235/prd.md` shipped at ≤50 lines against ADR-017 precedent ~150 lines and required pre-merge amendment to ≤150; H1 (Be Proactive — move catch upstream from `8-habit-reviewer` pre-PR pass into authoring step itself) + H5 (Seek First to Understand — measure precedent before authoring ceiling, same discipline as "read existing code before writing new code") + H7 (Sharpen the Saw — invest in the meta-tool `/requirements` that authors specs, P/PC balance per [ADR-018](docs/adr/ADR-018-memory-layer-activation.md)); H8 Modeling — apply friction-first to the authoring step itself; plugin boundary respected per memory obs #233270 — no validator extension, no PreToolUse hook (runtime enforcement belongs in `pitimon/claude-governance`); out-of-scope confirmed: no retroactive PRD audit, no extension to non-numeric FR fields; Forward-Guardrail Sunset 2026-11-24 per ADR-017 mechanism with reversal criteria (no `/reflect` cite, no calibrated FR in subsequent PRDs, FR-007 remains only friction signal); validator state: `validate-structure.sh` 358 PASS / 0 FAIL with Check 26 hygiene preserved via `because` reason marker; consumer-doctrine bump per ADR-019 (`skills/**` + `guides/**` edits MUST bump) atomic across 4 files; PR closes #237)
- v2.18.4: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Skill Authoring Guide — closes N1 (Pre-Building Preparation absent) + P2 (Objective conflated with trigger) gaps from 2026-05-24 Vibe Coding Thailand audit of Ben AI's 5-part framework; new `guides/skill-authoring.md` (~300 lines) covers Pre-Building Preparation pattern (draft 4 reference docs before SKILL.md, with `skills/calibrate/examples.md`+`skills/using-8-habits/examples.md` as precedent for human-curated `examples.md` companion) + canonical SKILL.md skeleton with dedicated `## Objective` section (distinct from `description` trigger-rubric per Check 25 and `**Habit**: H?` label) + authoring lifecycle (`/research` → reference docs → SKILL.md → `/reflect` → SKILL-EFFECTIVENESS feedback loop per ADR-018); `CONTRIBUTING.md` template diff inserts `## Objective` section after `**Habit**:` line + adds guide pointer at section head; ADR-020 records Tier 1 forward-guardrail ship per ADR-014/017 precedent; **cross-verify reconciliation** — source brief's first draft recommended "ship nothing" on friction-first grounds, `@8-habit-reviewer` scored 12/17 with 5 failures (H1 N1 dismissed without consistent precedent application, H5×2 P2 mapping stretched + N4 only maximal reading, H8×2 C4 source-tier downgrade-as-dismissal + "deliberately concludes nothing" self-absolution), revised verdict applies ADR-014/017 "ship with zero friction" precedent consistently — this is the SAME selective-strictness shape that rescued the ADR-017 draft 4 days earlier (lesson `2026-05-24-anthropic-5-pattern-audit-adr-017.md:25-26,33`); T2 deferred (N2 multi-option HITL ≥5; N4b per-skill `examples.md` extension) with drop date 2026-11-24; T3 rejected (N3 iteration-cycle framing low-value; N4a self-modifying skills charter conflict — same boundary ADR-017 used to reject Anthropic Pattern 4); consumer-doctrine bump per ADR-019 (`guides/**` MUST bump, even for "doctrine refinement" style change); Forward-Guardrail Sunset 2026-11-24 per ADR-017 mechanism (not ADR-016 eviction, which scopes only to never-shipped T2 items); habit mapping H4 (deposit — future contributors don't re-discover the methodology) + H5 (Pre-Building Preparation IS H5 applied to skill authoring) + H7 (closes documented Production-Capability gap — 23 skills with no authoring guide) + H8 Modeling (apply friction-first standard to ourselves the same way we apply it to incoming patterns); pattern: **advisor-then-reviewer-then-revise is load-bearing for external-pattern audits** — second consecutive 2026-05-24 rescue from "ship nothing" by 8-habit-reviewer cross-verify, both driven by inconsistent application of ADR-014 precedent; PR closes #235)
- v2.18.3: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Anthropic Engineering Doctrine Audit Guide — adds `guides/anthropic-engineering-doctrine-audit.md` as defensive citation surface for ADR-018 "Earn each line" doctrine; Table 1 catalogues 12 already-adopted patterns from Anthropic/Karpathy/Claude Code engineering posts with file:line citations (iterative `AskUserQuestion`, 4-layer prompt order, `<system-reminder>` runtime updates, cache-architecture awareness in `/build-brief`+`/design`+`/breakdown`, Karpathy folder-structured wiki via `~/.claude/lessons/INDEX.md`, `hot.md`-equivalent via SKILL-EFFECTIVENESS tally, periodic wiki linting via release checklist, skill 3-layer architecture via ADR-009, composability via 7-step DAG, post-session refinement via `/reflect` Q6, subagent context isolation via `@8-habit-reviewer`, audit-before-deep-dive via 2026-05-24 lesson); Table 2 catalogues 7 evaluated patterns (N1 HTML self-verify OOS plugin-boundary, N2 interactive `/requirements` T2 drop-2026-11-24, N3 per-skill token budget T2, N4 stable-prefix declaration OOS read-mostly content, N5 `hot-recent.md` rolling cache T2, **N6 skill-description routing audit T2 — reclassified from T1 after cross-verify flagged cherry-picking** (2026-04-22 lesson records `skills/RESOLVER.md` as proactive feature #135 not friction patch; evidence is n=0 not n=1 by ADR-015 standard), N7 the guide itself T1 Adopted self-reference); `@8-habit-reviewer` cross-verify 11/17 PASS with 1 CRITICAL (N6 cherry-picking) + 2 HIGH (Step 1+2 redundancy, blog-sourced Proposed entries = meta anti-pattern) + 3 MEDIUM (commit type, DoD measurability, Option D ranked below Option C/E); scope reduced from 3 PRs to 1 PR per reviewer findings; consumer-doctrine bump per ADR-019 line 99 (`guides/**` is consumer-facing reference content even when not currently Load'd by any skill); habit mapping H4 (deposit catalogue for future contributors) + H5 (read priors before proposing) + H8 (modeling — apply friction-first to ourselves); pattern: **defensive-citation surface as anti-dormancy mechanism for ADR-018** — when the doctrine says "Earn each line" the corollary is "make 'we already evaluated that' greppable so the next person doesn't burn cycles re-discovering"; PR closes #231)
- v2.18.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (ADR-019 Doctrine-Only Scope Refinement + validate-structure.sh Check 27 — splits ADR-017 §C5 doctrine-only into contributor-doctrine (`docs/adr/`, `CLAUDE.md`, `CONTRIBUTING.md`, `.github/`, `tests/`, `SELF-CHECK.md`, `AGENTS.md`, `llms.txt`) which preserves no-bump intent vs consumer-doctrine (`rules/`, `skills/`, `hooks/`, `habits/`, `guides/`, `agents/`) which MUST bump + CHANGELOG even if change style is "doctrine refinement"; closes silent user-facing shift gap that would have hit `rules/effective-development.md` next Earn-each-line audit per ADR-018 §"Context"; Check 27 mechanical enforcement: git diff against last release tag, if consumer-doctrine path touched AND version unbumped → FAIL with ADR-019 citation, skipped if no tag exists (first-release case); ratification of falsifiability frame from lesson `2026-05-24-notebooklm-corpus-completeness-falsifiability-frame.md` applied to ADR-017 §C5 ("what specific finding would falsify the decision? = a doctrine-only PR that silently changes consumer behavior — recognized one PR away, fixed proactively rather than wait for bug"); v2.18.2 itself ships as contributor-doctrine (ADR-019 + Check 27 + ADR-017 cross-ref note + CHANGELOG) — bump is elective because new validator = meta-change to CI behavior contributors should be signalled; Forward-Guardrail Sunset 2026-11-24 with reversal criteria (0 fires → too conservative, bypass without amendment → too rigid, no consumer-doctrine PR shipped → speculation); H1 (proactive close gap before next PR fires it) + H4 (deposit — consumers get signal when behavior changes); validator state: validate-structure.sh 358 PASS / 0 FAIL with Check 27 PASSing "contributor-doctrine only since v2.18.1 — no bump required (ADR-019)" on self-test; PR #230 → closes issue #229)
- v2.18.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Anthropic Skills 5-Pattern Audit — ADR-017 records Tier 1 P3 ship (validate-structure.sh Check 26 warning-only + `/scrutinize` Operating Rules 6 MUST/NEVER+Why blocks + `/diagnose` Phase 6 MUST re-run feedback loop citing Anthropic pptx ~line 243), P4 (embedded scripts) OOS per plugin charter, P5 split T2 bag drop 2026-11-23 + companion pitimon/claude-governance#37 for runtime PostToolUse hook; 8-habit-reviewer cross-verify caught selective-strictness asymmetry vs ADR-014 precedent (4 patterns shipped with zero friction 4 days earlier) and forced reconciliation paragraph promoting P3 from "ship nothing" to Tier 1 forward guardrail before PR opened; research-verifier verified 7/7 Anthropic citation texts verbatim (URL prefix `document-skills/` corrected to `skills/`, 4/7 pptx line numbers off by 40-61 documented as older-commit); validator state: validate-structure.sh 357 PASS / 0 FAIL (Check 26 flags /post-mortem + /reflect informationally non-blocking), validate-content.sh PASS after adding `## Options Considered` section to ADR-017 (caught by CI on first push, fixed in second commit); pattern: **two-layer challenge — advisor catches process gaps + reviewer catches substance gaps**; PR #219 merged as 8540f9e, issue #218 closed with H4 deposit summary, claude-governance#37 open for cross-plugin half)
- v2.18.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (`/diagnose` Skill: Friction-Driven External Adoption — ships single new skill `/diagnose` (1202 words, F3 safe) ported from mattpocock/skills SHA b8be62ff as a friction-driven adoption per ADR-015; 6-phase methodology (feedback-loop → reproduce → hypothesise → instrument → fix-with-regression-test → cleanup) closes documented gap between `/research` (too broad) and `/post-mortem` (too late); n=1 friction signal — lesson `2026-04-12-compression-worker-420-investigation.md` first-person retrospective explicitly states "Most useful: n/a (no 8-habit skills invoked during the fix session)" and "Could have been found in 5 minutes [via SQL comparison] vs 30 minutes of log analysis", strongest available citation form despite being below ADR-014's preferred n≥2 bar; honest framing recorded in ADR-015 with 4 future-reversal conditions (zero recorded uses at 6mo → consider deprecation; n≥2 friction → validates retroactively; user confusion vs /post-mortem → revise boundary; upstream SHA churn → re-audit); sets precedent for friction-first external prior-art adoption distinct from v2.17.0's pattern-first ADR-014 doctrine; SKILL.md adopts Decision-1 split (SKILL.md ≤2000w + reference.md unbounded) per ADR-009 precedent, Decision-2 Bash inclusion with read-only scope guardrail for Phase 1 feedback-loop construction, Decision-3 user-supplied path for FR-014 predecessor brief detection (no filesystem auto-glob), Decision-4 ADR-vs-design separation (ADR-015 records adoption only, design.md carries decisions), Decision-5 adapt-with-attribution preserving 6 phases + verbatim FR-004 hypothesis-template quote anchor for methodology fidelity; spec chain persisted at `docs/specs/diagnose-skill-v2-18-0/{prd,design,tasks}.md` (14 EARS criteria FR-001..014, 5 decisions 3 sticky, 8 atomic tasks with full FR-coverage matrix); research brief at `~/.claude/plans/deep-mattpocock-skills-second-pass-2026-05-23.md` (Deep-mode, 12/14 sources verified by research-verifier agent, all source URLs SHA-pinned for citation-rot resistance); cross-verify on research brief 8/11 applicable = 72.7% Mostly Ready raw, 90.9% Well-Prepared after Body fixes (SHA pinning + brief persistence + lesson capture deferred to post-merge /reflect); cross-verify on PRD 13/13 applicable = 100% all 4 dimensions, Body dimension rebounded from 25% research stage to 100% PRD stage validating ADR-014's "pin SHA + persist brief" discipline; consistency-check on PRD↔design↔tasks 0 CRITICAL/HIGH/MEDIUM 1 LOW (Decision-5 marker format inline-title — fixed before T1 implementation); plugin total: 23 skills; validator state: validate-structure.sh + validate-content.sh + test-skill-graph.sh + test-verbosity-hook.sh all green post-fix-loop; H1 (Be Proactive — prevent recurrence not just patch symptom) + H5 (Seek First to Understand — reproduce before fix); pattern: **friction-first audit beats pattern-first audit** — when ADR-014's premise-inversion lesson is operationalized in a follow-up cycle, the resulting adoption stands on first-person retrospective evidence not third-party pattern attractiveness, even at n=1)
- v2.17.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (External Prior-Art Audit — adopts 4 patterns from mattpocock/skills as forward guardrails: P1 AGENT-BRIEF template, P3 `disable-model-invocation` frontmatter, P4-lite `docs/out-of-scope/` catalog, P5 description rubric validator Checks 24+25; honest framing in ADR-014 records that all 4 adoptions ship without prior friction-signal evidence — forward guardrails not fixes for observed weakness; advisor pattern critique mid-flow surfaced the premise-inversion lesson and was overridden via `/goal` directive after R1 harness verification proved zero-break risk (anthropics/claude-code#22345 OPEN means decoration only for plugin skills, two regressions #26251 and #43875 already CLOSED); P5 pre-shipment audit found 0/19 drift — Check 25 activates as forward guardrail with empirically-grounded trigger-phrase regex; 3 OOS seed entries (brainstorm-removed, agentskills-no-go, eu-ai-act-migrated) preserve real rejection rationale with YAML frontmatter schema; CONTRIBUTING.md explains ADR-vs-OOS verb distinction (R3 mitigation); /breakdown Handoff narrows FR-006 from event-driven to ubiquitous (variance documented in design.md); validator state: validate-structure.sh 293+/0 PASS, all 4 suites green; H5+H7 — external audit as renewal discipline; spec chain persisted at docs/specs/mattpocock-t1-v2-17-0/{prd,design,tasks}.md, research brief at ~/.claude/plans/deep-https-github-com-mattpocock-skills-glimmering-prism.md verified 13/14 sources via research-verifier; pattern: forward-guardrails ship with honest disclosure beats deferral when the upstream regression risk is empirically zero)
- v2.16.5: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (Companion Announcement: devsecops `/workflow` → `/security-workflow` — docs-only patch closing the paired-announcement promise from `devsecops-ai-team` v10.12.0's CHANGELOG; `docs/INTEGRATION.md` "Tested versions" devsecops peg bumped 10.10.0 → 10.12.0+ (stale by 2 releases: v10.11.0 `/runner-audit`, v10.11.1 QA patch, v10.12.0 `/workflow` rename, v10.12.1 cross-refs) with inline note explaining the cross-plugin collision and how it was resolved (devsecops side renamed; this plugin's `/workflow` keeps the generic name as it IS the 7-step Covey practice); `skills/workflow/SKILL.md` gains "See Also (Cross-Plugin)" footer pointing users who meant scan-orchestration to devsecops `/security-workflow` directly from SKILL.md instead of forcing them to read INTEGRATION.md mid-workflow; `README.md` Companion Plugins section updates tested-version peg + adds naming-note callout for users searching by old name; companion PR `devsecops-ai-team` v10.12.1 (paired) ships symmetric README "Companion Plugins" section + skill-level "See also" callouts in `/eu-ai-act-assess` (Article 10) ↔ claude-governance `/eu-ai-act-check` (Article 14) and `/security-workflow` ↔ this plugin's `/workflow`; pattern: **cross-plugin companion-announcement = part of DoD for any cross-plugin slash-command rename, not a follow-up task** — captured in post-release `/reflect` lesson `cross-plugin-companion-announcement-pattern`; H4 reciprocity discipline — every interaction is a deposit, paired-release closes the v10.12.0 → v10.12.1 1-day stale-doc gap; validator state: validate-structure.sh PASS, validate-content.sh PASS / 0 FAIL / 1 WARN / 0 fitness breaches)
- v2.16.4: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#207 `/save-spec` Suite-Positioning Honesty Patch — Adopter #2 third-repo test in an operational VA/PT workspace with `claude-mem` active (29 obs / 10.5K-token SessionStart injection) + 284-line CLAUDE.md surfaced two real overlap cases the docs didn't acknowledge: P1 (skill positioned as "read-first save point after /clear" overstates value when memory-MCP already injects post-/clear context AND CLAUDE.md is short enough to scan in <30s — §4 becomes the only section providing net value), P2 (suite-positioning: skill listed alongside Assessment skills in README + using-8-habits/reference suggested workflow-step ordering it doesn't have); v2.16.4 ships both docs-only fixes (P1 + P2) + explicitly defers P3 (`--skip-empty-sections` flag) per adopter recommendation; SKILL.md "When to Skip" gains memory-MCP-overlap entry with 30-second-CLAUDE.md-scan threshold + new "Suite positioning (not a workflow step)" section explicitly framing the skill as a deployment-mode helper orthogonal to the 7-step workflow (prev-skill: any / next-skill: any) alongside `/calibrate` + `/reflect` as state-write skills run on user demand; README skill-table row reclassified to lead with "Deployment-mode helper (not a workflow step)"; using-8-habits/reference.md row reclassified with the same framing + skill-category note; RESOLVER triggers verified unchanged (user-invocation intent unchanged; only skip-criteria changed); **arc-close criterion validated**: v2.16.3 said "round 6 deferred unless a third independent adopter surfaces friction" — that condition triggered within ~2 hours of the v2.16.3 release; the criterion was right, the timing estimate was over-optimistic; pattern continues at n=3 evidence base (netbox-sit + netbird-sit + operational VA/PT workspace); pre-PR self-test (DoD-must-execute action item) executed: grep-verify all 4 new sites present (Memory-MCP entry / Suite positioning / README Deployment-mode helper / using-8-habits Deployment-mode helper), validators clean; **convergence pattern continues**: adopter's own /cross-verify on this issue scored 13/15 = 86.7% Mostly Ready, maintainer's /cross-verify on the implementation posture scored 15/15 = 100% Well Prepared — adopter modest, maintainer agrees; pattern: **H8 Conscience applied to marketing copy** — the SKILL.md's own H8 checkpoint admitted "the value depends on the user's habit of updating it"; this release extends that honesty to "When to Skip", because over-promising in marketing is the same anti-pattern the H8 checkpoint guards against in operations; validator state: validate-structure.sh 268/268 PASS, validate-content.sh 220+ PASS / 0 FAIL / 1 WARN / 0 fitness breaches; P3 `--skip-empty-sections` flag explicitly deferred per adopter recommendation ("fix the docs first, see if anyone actually requests this"); 6-round arc CONTINUES — closure deferred to round 7 unless adopter signal stabilizes)
- v2.16.3: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#205 `/save-spec` Round-5 Arc-Close Polish — Adopter #2 closure pass on v2.16.2 verified F1+F2+F3 fixes hold in real use, surfaced 1 MEDIUM bug + 2 LOW items + arc-close meta; **R5-3 (MEDIUM bug, fixed)**: scaffolded SPEC.md §2 markdown table rendered broken on every empty-decisions scaffold because `reference.md:30` blank line separated alignment row from `<§2-rows>` substitution marker — GitHub/mdast renderers treated as degenerate header-only table + stray-pipe paragraph; fix initially attempted by removing blank + adding HTML-comment assembly directive, but the project formatter (Prettier-class) **persistently re-wedged blank lines around HTML-blocks AND `<...>` markers**, making the standalone-marker approach impossible to keep stable; final fix uses **table-row-shaped substitution marker** (`| §2-rows-ASSEMBLY-DIRECTIVE | ... | ... | ... |`) so the formatter sees it as a real table data row (which it does NOT wedge blanks around); generator replaces this row with actual rows at scaffold time; new `tests/validate-content.sh` Check 12c.1 regression added with flexible alignment-row regex (formatter-padding-tolerant) that catches blank-line-before-data-row recurrence; **R5-1 (LOW-MEDIUM doc, fixed)**: template assembly markers at `reference.md:22,31,38` previously used `<§N-...>` angle-bracket syntax visually identical to F1-class pre-fix placeholders, risking future contributor confusion ("didn't we fix these?") — fixed by consolidating the assembly-directive intent INTO the marker text itself with explicit `ASSEMBLY-DIRECTIVE` capitalized phrase + "NEVER appears in output — replaced by generator at scaffold time" language; §1/§3 use angle-bracket form (formatter-tolerant for list contexts), §2 uses table-row form (formatter-required for table contexts); **R5-2 (LOW doc, fixed)**: FR-017 error template previously reused Decision-4 Write-failure template ("Tried to create SPEC.md at...") for the wrong register — adopter who typos a target-dir path got permission/disk troubleshooting wording instead of "directory not found" guidance; added new **canonical pre-flight error template** in reference.md ("Directory not found: <target-dir>...") with adopter-correct register; FR-017 wired to point at new template, NOT Decision-4; SKILL.md Process step 1 updated to emit pre-flight error before any Glob/Write call; **F3 follow-on (DoD-must-execute validation)**: pre-PR self-test (action item from /reflect lesson 2026-05-17) executed against synthesized v2.16.3 scaffold output — all 5 verification commands pass, F1 regression check (no angle-brackets in output) passes, R5-1 regression check (no `<§N-...>` in output) passes, R5-3 §2-table-adjacency check passes; self-test discipline **caught zero bugs this round** (the previous round caught F3 BSD-awk regression), confirming the lessons → action-item → self-test → caught-zero-bugs convergence pattern — system is now systematically preventing what it previously discovered reactively; validator state: validate-structure.sh 268/268 PASS, validate-content.sh 220+ PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: **formatter-vs-substitution-marker arms race resolved via table-row-shaped marker** — when a template marker must be adjacent to a markdown construct that's formatter-stable (table data rows), make the marker itself look like that construct; the formatter then treats it as a peer of the surrounding structure, not as an HTML-block to be padded; 5-round adopter-maintainer rhythm closed per Adopter #2 recommendation — defer round 6 unless a third independent adopter surfaces real friction)
- v2.16.2: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#203 `/save-spec` Phase 1 Round-3 Polish — Adopter #3 dogfood report from real `/save-spec` execution on `~/va+pentest` (per-engagement VA/Pentest workspace) surfaced **F1 (MEDIUM bug)**: 6 angle-bracket placeholder sites shipped unfilled in scaffolded `SPEC.md`, contradicting the read-first-context purpose — fixed via hybrid approach: §2/§3 skip-stub rows use plain prose italic markers (`_no decisions seeded yet — add the first one when it lands_`); §1 narrative + §4 fill-required + §4 "Last apply / commit / deploy event" + §4 "What's happening now" + §4 "Optional command sequence" all converted to `<!-- TODO: ... -->` HTML comments (invisible at render, visible to editor); `tests/validate-content.sh` Check 12c whitelist extended to `skills/save-spec/` with Exception 2 comment explaining the intentional fill-me-in signals (parallel to existing Exception 1 for consistency-check); **F2 (LOW-MEDIUM friction)**: no `--target-dir` argument → multi-repo adopters had to session-switch per repo — fixed by adding second positional argument `[target-dir]` to `argument-hint`, Process steps 1/2/6/8 now resolve `<target-dir>` (defaulting to cwd if absent); PRD FR-013 scope-clarified, new FR-017 added for target-dir semantics; **W2 (doc softening)**: N2 timestamp warning in reference.md updated with verified-working note (Adopter #3 fresh-session scaffold produced `2026-05-17T22:46:06+07:00` correct Bangkok offset — failure mode reframed as "documented escape-hatch, not expected outcome"); **F3 (MEDIUM-bonus surfaced by pre-PR self-test)**: discovered that `guides/spec-digest-pattern.md` Check 2's `awk '/^## 4\. Current state/,/^## /'` range collapses to 1 line on BSD awk (macOS default) because the end-regex `^## ` matches the start line `## 4. Current state` too — replaced with `sed -n '/^## 4\. Current state/,/^## /p'` which has correct cross-platform semantics; the action item from the same-day /reflect lesson 2026-05-17-maturity-ladder-dogfood-arc-v2-16-1.md ("literal execution of verification commands before any new-skill PR opens") **demonstrably worked** — F3 was caught by literal execution, would never have surfaced via static review; positive verifications from Adopter #3: W1 N3 free-text affordance works in real use, W2 N2 timestamp verified, W3 refusal-path safety guard works; validator state: validate-structure.sh 268/268 PASS, validate-content.sh 219+ PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: **DoD-must-execute principle empirically validated within 24h of being coined** — the v2.16.1 /reflect action item caught its own first regression test; same-day discovery, same-day fix, no waiting for external adopter to hit BSD-awk shells)
- v2.16.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#201 `/save-spec` Phase 1 Polish — Adopter #2 dogfood review surfaced 1 correctness bug + 3 quality items, all fixed in single patch PR per maturity-ladder discipline (correctness-first, then UX); **N1 (MEDIUM bug)** — §1 empty stub used `` `<filename>.md` `` which Check 4's backtick-path grep extracted as `<filename>.md` (literal angle brackets), failing `[ -e ]` and emitting `MISS  <filename>.md`; the Definition of Done's claim that "output passes the 5 verification commands" was provably false on default scaffold — fixed by changing stub to plain prose `_§1 is empty — add project-specific pointers as the repo grows._` with no backticked .md path; **N2 (LOW)** — RFC 3339 timestamp documented as wrong-offset risk when skill (no `Bash`) generates value from session-injected `<system-reminder>Current:</system-reminder>` context that may be absent in non-interactive runtimes; reference.md now includes adopter guidance (verify offset after scaffold, edit manually if wrong) + Phase 2 hook for adding `Bash` if feedback shows unreliability; **N3 (LOW UX)** — Q2 added "Other" free-text affordance accepting newline-separated paths for ops/infra repos with project-specific names (`server-state.md`, `playbooks/change-management.md`, etc.) that have zero overlap with the 5 canonical glob names; Example F added to reference.md showing the parse flow; **N4 (LOW doc)** — PRD FR-003 deduplicated → references reference.md as canonical for the refusal message (single source of truth, paraphrased inline draft was a drift hazard); validator state preserved: validate-structure.sh 268/268 PASS, validate-content.sh 219+ PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: **patch-release Adopter #2 dogfood discipline** — adopter #2 reports surfaced N1 in <2 hours after the v2.16.0 release; correctness fix shipped same day; the dogfood feedback loop is faster than the original promotion criteria gathering because the skill itself enables more rapid scaffold-and-test iterations; #197 closeable (5/5 items addressed in v2.16.0 + #198))
- v2.16.0: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#199 `/save-spec` Skill Promoted — Phase 1 Minimum Viable — promotes the v2.15.9 spec-digest-pattern guide to a user-invocable skill (`skills/save-spec/SKILL.md` + `reference.md`) after all three documented promotion criteria were objectively met: n=2 independent adoptions (netbox-sit + netbird-sit via #197), scope question closed in writing (the two deployment modes are disjoint in practice — multi-mode repos out of scope for tooling), and `/reflect` lesson 2026-05-17-spec-digest-pattern-v2-15-9.md + #197 adopter friction captured; skill is **generator-only Phase 1** (refuses to overwrite an existing SPEC.md; Phase 2 `--update` flag deferred); 8-step Process section pinned by new `tests/validate-structure.sh` Check 23 as sticky per Decision-7 (drift requires new /design cycle); hybrid auto-detect globs 5 canonical pointer-target files (PLAYBOOK/CONTRACTS/LESSONS/CHANGELOG/README.md, case-sensitive) and asks user to confirm §1; 4-prompt interactive flow (project name + §1 multi-select + §2 comma-separated free-text + §3 comma-separated free-text) with skip-sentinels (skip/none/nothing/n/a/empty); §4 timestamp uses RFC 3339 strict with local timezone offset; emits CLAUDE.md auto-update recipe stanza to conversation only — does NOT modify CLAUDE.md (FR-014, FR-015); ADR-013 receives 2026-05-17 follow-up note within existing addendum stating user-invoked write stays outside Alt-4 auto-write-hook rejection scope; `guides/spec-digest-pattern.md` "Promotion to a skill (deferred)" section rewritten to "Promoted to /save-spec in v2.16.0" with the explicit scope-resolution statement; dogfooded the `--persist <slug>` convention (PRD/design/tasks at `docs/specs/save-spec/`) on the very feature that scaffolds the alternative project-orientation mode — cross-mode dogfooding; `/consistency-check save-spec` ran clean (0 CRITICAL, 0 HIGH, 0 MEDIUM, 4 LOW for missing alternatives markers in Decisions 7-10 — accepted as informational per Significance profile avoid-documentation-gold-plating stance); validator state: validate-structure.sh 266/266 PASS (Check 23 adds 8 assertions to baseline 256), validate-content.sh 217+ PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: **promotion via maturity ladder** — decision-driven, data-backed; the v2.15.9 guide's promotion criteria section made the next escalation step explicit and falsifiable; n=2 evidence converted "speculative skill" into "criterion-driven ship")
- v2.15.9: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#194 Project-Orientation Hub Mode Documentation — PR #195 docs-only addition introducing `guides/spec-digest-pattern.md` (~180 lines) documenting the project-root `SPEC.md` digest pattern as deployment-mode complement to v2.15.2's per-feature `current-state.md` (feature-spec mode); template paraphrased from production artifact `scanopy/netbox-sit/SPEC.md` (153 lines) that independently arrived at four-section save-point shape after `/clear`/`/compact` flushing pain; ADR-013 receives additive `2026-05-17` addendum clarifying its rejections cover feature-spec mode only (Alt-1 unified prd+design+tasks merge, Alt-4 always-on auto-write hook, CHANGELOG v2.15.0 `/save-point` skill rejection) and do NOT govern the digest-layer-above-detail-files archetype netbox-sit represents; `/save-spec <slug>` skill explicitly deferred pending ≥2 independent project adoption signal per working-with-pitimon "minimal additions, user-demand-driven" stance + PR #111 local-maximum lesson; cross-links from `guides/persistence-convention.md` (the two modes are complementary not conflicting) + `README.md` Use Cases row "Survive `/clear` and `/compact`"; habit mapping H5 (3-agent research + advisor + 8-habit-reviewer before proposing, addendum honestly reflects original ADR scope), H2 (acceptance criteria + out-of-scope list in #194 before code), H8 (plugin recognizes two legitimate deployment modes), H3 (guide-first lowest-commitment reversible path); 8-habit-reviewer caught nested-fence rendering issue (outer ```markdown collapsed to literal `\\`\\`\\`bash` inside template) pre-merge — fixed to 4-backtick outer fence; post-merge CI surfaced second issue — `validate-content.sh` Check 12b naive `[X](X)` link resolver flagged 8 template-internal placeholder links inside the SPEC.md code block as broken (validator not backtick-aware); template rewritten to use `` `PLAYBOOK.md` `/` `CONTRACTS.md` ``etc instead of`[X](X)` syntax with note telling copy-paster to linkify in their own project — same fix applied to Decisions table example row; validator state: validate-structure.sh 256/256 PASS, validate-content.sh 216 PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: empirical-evidence-driven discipline addition — real-world artifact from another session revealed second deployment mode, plan revised twice (after reviewer + advisor) to guide-first commitment level matching n=1 evidence, deferred skill until adoption signal confirms pattern repeats)
- v2.15.8: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#191 `/reflect` Auto-Consolidation UX — PR #192 skill-only patch removing two-step nudge + invoke friction from Step 7; `count > 10` now auto-runs Orient → Gather → Consolidate inline; no-merge result → INDEX.md updated automatically + 1-line summary; deletion proposals still gate on explicit user approval (In-the-Loop per ADR-002 preserved); explicit `/reflect consolidate` argument retained for verbose output; DoD bullet 5 updated to "auto-ran" (testable outcome vs "check performed" which was vague); 8-habit-reviewer pass: governance gate verified at lines 102-104 "STOP. Do not write or delete anything until the user gives explicit approval" — correctly scoped to deletion-only; H7 alignment verified against `habits/h7-sharpen-saw.md` Rule 2 "What could be automated?" — this is PC² applied to the reflection loop itself; all frontmatter/Load directives/other sections unchanged confirmed by diff; validator state: validate-structure.sh 256/256 PASS, validate-content.sh 217 PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: PC² — invest in the capability that builds capability; root cause was threshold 10 that mature repos cross quickly — fix makes auto-run safe for common case (additive INDEX update) while preserving gate for rare case (irreversible deletions))
- v2.15.7: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#188 Vendor Portability Discipline for Managed Agent Platforms — PR #189 doc-only addition introducing `guides/vendor-portability.md` (90 lines) responding to industry move toward managed-agent runtime features (Claude Managed Agents, OpenAI Assistants, Bedrock Agents) with three principles: persist artifacts outside vendor (repo = source of truth) + treat managed memory as cache not source of truth + separate discipline portable from runtime vendor-specific; selection checklist framed as `/cross-verify` Q14 "third alternative beyond the obvious options" exercise (managed vs. self-hosted rarely binary, hybrid with persistence discipline often the better answer); habit mapping H8 Voice architectural autonomy / Spirit dimension as primary anchor, H1 Proactive prevent lock-in / Body, H4 Win-Win Emotional Bank Account artifacts that inform the next person / Heart (canonical framing verified against `habits/h4-win-win.md`), H7 Sharpen the Saw reproducibility = PC over P / Mind — full Whole Person 4-dimension coverage; `llms.txt` indexed under Philosophy section alongside `integrity-principles.md`; vendor neutrality verified — Claude Managed Agents + OpenAI Assistants + Bedrock Agents cited together with no platform-favoring framing; plugin boundary preserved — workflow discipline not enforcement, no compliance framework mapping, no PreToolUse hook (those belong in `claude-governance`); 8-habit-reviewer pre-commit dispatch caught Commandment #13 violation — initial draft mislabeled Q14 as "External dependencies" but actual text per `guides/cross-verification.md:45` is "Have I considered a third alternative beyond the obvious options?", H4 mapping reframed from stretch "audit-ready record" to canonical "artifacts that inform the next person" — both fixes pre-commit demonstrating the integrity discipline applies to its own writing; validator state: validate-structure.sh 256/256 PASS, validate-content.sh 217 PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: discipline answer to a runtime trend — framework responds to vendor convergence not by replicating features but by naming the portability discipline that keeps users vendor-neutral)
- v2.15.6: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#153 SKILL_OUTPUT Producer + Consumer Doc Sync — PR #186 doc-only patch closing pair of same-shape drift gaps in `guides/structured-output-protocol.md` both stemming from `/design`'s `SKILL_OUTPUT:design` block added without doc sync; producer entry inserted between `/requirements` and `/breakdown` (workflow Step 2 placement matching skill DAG), schema mirrors `skills/design/SKILL.md:128-142` exactly with all 7 keys (`decision_count`, `decisions`, `sticky_decisions`, `constraints`, `adr_references`, `article_14_applicable`, `article_14_pass`) using concrete example values matching existing producer style (`ears_count: 5`, `task_count: 5`, `files_reviewed: 4`); consumer entries added — Q14 (`decision_count` → third-alternative flag, fires when only 1 option presented) + Q16 (`sticky_decisions` → WHY-not-captured flag, fires on 0 sticky in design with >3 decisions) + Q4 extended with design-block cross-check (`decision_count` vs `success_criteria_count`), all 5 SKILL_OUTPUT-consuming questions from `skills/cross-verify/SKILL.md:35-41` now mirrored in the guide; pattern: producer + consumer doc-sync-as-a-pair — adding producer alone would have shipped half the gap and itself created the "confusion point" the issue cites, H4 + H1 closure fixes both halves in single PR; surfaced originally by 8-habit-reviewer cross-verification of PR #152 (#151 attribution implementation) as pre-existing gap out of scope for #151; validator state: validate-structure.sh 256/256 PASS, validate-content.sh 217 PASS / 0 FAIL / 0 fitness breaches; CI link-check PASS on producer-only PR plus release-tag exclude from v2.15.5 prevents chicken-and-egg failure)
- v2.15.5: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#172 Repo-Wide Link-Check CI Gate + 2 Real Link-Rot Fixes — PR #184 adds `.github/workflows/link-check.yml` using lychee (Rust, pinned commit hash matching wiki-linkcheck for SHA SSO), runs on PR + push to main when `**/*.md` changes, scope external HTTP/HTTPS URLs across all `*.md` outside `docs/wiki/` (wiki covered by separate workflow with different wiki-style link semantics), internal markdown links remain validated by `tests/validate-content.sh` Check 12b — two-layer design covers full surface without duplication; excludes self-referential `pitimon/8-habit-ai-dev/(blob|tree|raw)/main/` (only resolves post-merge) + private cross-repo `pitimon/(memforge|devsecops-ai-team)` (workflow GITHUB_TOKEN scoped to this repo only, cannot authenticate against other private repos), `claude-governance` is public and stays in scope; first CI run on PR #184 caught 3 real link-rot issues — README.md:666 typo `claud-mem-me`→`memforge` (clear bug — repo `pitimon/claud-mem-me` doesn't exist), ADR-005:137 `https://ai-act-service-desk.ec.europa.eu/en/ai-act/` returns 404 (EC restructured service desk path, ADR-005 is Superseded per ADR-012 so preserved as historical reference text with note), `pitimon/devsecops-ai-team/issues/467` 404 due to private-repo token scope (now excluded via regex pattern); CONTRIBUTING.md gains "Link check (external URLs)" subsection under Testing Conventions documenting both link-check workflows + two-layer design; pattern: CI gate that immediately proves its own value — first run caught 3 real issues, demonstrating link rot was already happening silently and the gate catches it at PR time vs when users report broken navigation; 8-habit-reviewer recommendation from 3-plugin integration audit (PR #171/#32/#468) now enforced; validator state: validate-structure.sh 256/256 PASS, validate-content.sh 217 PASS / 0 FAIL / 0 fitness breaches; admin-merged via branch protection per release convention)
- v2.15.4: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#167 Backtick-Aware Ambiguity Pass + Dogfood ID Cleanup — PR #182 first true bug-fix patch in v2.15.x line addressing CRITICAL false-positive surfaced by v2.15.0 dogfood smoke test 9 days prior; `/consistency-check` Pass 3 gains "Backtick-context filter (required)" subsection per Option A from #167 — pre-strip backtick-quoted segments from each line before applying [NEEDS CLARIFICATION]/TBD/TODO/???/XXX token match, covers single-backtick inline spans + triple-backtick fenced blocks, eliminates `docs/specs/consistency-check/prd.md:45` false-positive where PRD legitimately mentions token as detection-target documentation; aligns analyzer runtime semantics with validator-side whitelist already in `tests/validate-content.sh` Check 12c (two-tier design now internally consistent — tokens in backticks are documentation-references everywhere); known-limitation note removed from `skills/consistency-check/reference.md` (limitation was the bug, bug is now fixed); F2 residual closed — `tasks.md:48` `Decision-D5`/`Decision-D9` → canonical `Decision-5`/`Decision-9` per ADR-013, completes PR #169's earlier canonicalization pass; Check 21 added asserting 3 contract signals ("Backtick-context filter" label + "documentation-references" semantic + "pre-strip" workflow) — prevents future drift back to plain `grep -nE` semantics; validator state: validate-structure.sh 256/256 PASS, validate-content.sh 217 PASS / 0 FAIL / 1 WARN / 0 fitness breaches; pattern: bug fix of feature shipped 9 days ago — distinct from v2.15.1/2/3 content additions and convention imports; admin-merged via branch protection per release convention)
- v2.15.3: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#179 Integrity Commandment #13 — Grep-Verify Quotes Before Pasting — PR #180 content-only addition to `guides/integrity-principles.md` closing verification-discipline gap surfaced in v2.15.2 reflection (obs #85070, #85071); commandment #13 added under Honesty & Accuracy covers ADR citations, habit principle claims, scare-quoted external phrases, observation IDs, prior-conversation paraphrases presented as direct quotes — trigger was habit-attribution drift detected across two consecutive PR reviews (#174 Spirit/Conscience → corrected to H1+H5; #177 H2 → corrected to H3) + quote misattribution `"Magic" behavior` at ADR-013 Alt-2 line 87 wrongly cited to Alt-4 propagated through 4 artifacts before reviewer catch; mapping table extended `5-7, 13 (Honesty)` → H8 Find Voice / Spirit (conscience); 3 live cross-refs synced (title 12→13, README architecture-tree comment, `/review-ai` load directive) while historical entries (v1.9.0 release line, v2.0.0 CHANGELOG delta, SELF-CHECK v1.9.0 improvements section) intentionally preserved as period record; dogfooding caught meta-violation pre-commit — initial text quoted `"magic behavior"` (lowercase) but ADR-013:87 says `"Magic" behavior` (capitalized, scare-quote on single word), self-corrected demonstrating the rule applies to its own writing; pattern: commandment growth driven by reflection-detected internal drift across ≥2 sessions — distinct from v2.15.1 upstream-import and v2.15.2 community-article convention-import; validator 256 PASS / 0 FAIL)
- v2.15.1: Body 5, Mind 5, Heart 5, Spirit 5 = **5.0** (#173 Doubt-Driven Techniques Imported — PR #174 single-guide enhancement to `guides/advisor-pattern.md` importing 3 techniques from `addyosmani/agent-skills` doubt-driven-development skill (MIT, PR #139, upstream 2026-05-07 — 27 days after prior addyosmani audit cutoff in PR #111): anti-CLAIM-bias rule (H5), iterative 3-cycle stop cap (H3+H7 conditional), adversarial prompt template (H1+H5) dispatching fresh no-named-role subagent rather than `@8-habit-reviewer` (whose 17-question process is fixed per `agents/8-habit-reviewer.md:18-24`); Quick Reference gains second row for adversarial pattern, See Also cites MIT upstream; honors PR #111's local-maximum lesson — no new skill/agent/validator; rejected in-scope candidates explicit in #173 body (new /doubt-check skill, source-driven-development import, cross-model CLI escalation, agentskills.io frontmatter migration ADR-007 NO-GO unchanged); `@8-habit-reviewer` agent dispatched on diff pre-merge: 14/17 pass + 3 N/A + 0 hard blockers + 3 polish items addressed in-PR (F1 Quick Reference row, F2 concrete subagent dispatch syntax, F3 missing habit label) — pattern: post-audit delta evaluation rather than full re-audit on upstream methodology innovation)

---

_Updated with each release. Previous: 2.21.32 (Body 5, Mind 4, Heart 4, Spirit 5 = 4.5)_
